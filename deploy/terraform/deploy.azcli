terraform plan --out=main.tfplan
terraform apply main.tfplan

cd ../../src
npm install
npm run build
zip -r ../deploy/terraform/build.zip *
cd ../deploy/terraform

az webapp deploy --resource-group "WordsGroup" --name "wordsapi" --src-path ./build.zip
